# 三思而后行 · 改前先通读

[English](./README.en.md)

改任何已成形的文档之前，先通读全文骨架再动手。

## 它是干嘛的

它挡在「要动手改一份已有文档」这个动作前面：不管你是改一段、补一节、按评审意见改稿，还是被丢一整篇说「优化一下」，它都要求你先把整篇读完、看清骨架，判断这一处该怎么改才不破坏整体，然后再下笔。它只管文档类产物（文章、调研、方案、技能文档），不碰代码。

## 为什么要用它

改文档最普遍、也最隐蔽的毛病，是**只盯着要改的那一处，不看整体**。

你收到一条评审意见说某段有问题，很自然就冲上去改那一段；用户说「顺手加一段」，你就把新内容挂在末尾。问题是，一份成形的文档是一个有骨架的整体——每一节承接上一节、论据分布在特定位置、结论要收住前面所有铺垫。你只改局部，看不到这一处在骨架里的位置，就会一次次埋下问题：

- 改完这段，和上一段、下一段接不上了，读起来断片。
- 新增的一节挂在末尾，其实和主线不搭，成了游离的补丁。
- 把上一次评审刚删掉的内容又加了回来，因为你没看历史。
- 说是「优化」，结果只逐句抠措辞，论据放错节、结论收不全这类骨架病全漏了。
- 一遍改一遍看效果、来回反复——这本身就是没通读整体的症状。

Agent 尤其容易犯这个毛病。它的默认反应是**看到一个点就修一个点**：评审指出一处就改一处，检查命中一条就消掉一条。每次局部修改看着都合理，几轮叠起来却可能把同一件事写重、把上次删掉的东西加回来，最后越改越散。这个技能把「改前先想一下」变成 Agent 真能执行的流程，让它在动手前停一下，先看整体、列影响面，再决定该不该改。

这些毛病不属于任何一个具体的写作技能，所以单开一个技能来管它。**它的思路借自四条已经立住的工程和写作经验**，只是搬到了文档修改上：

- **Shotgun Surgery**（改一处要连带看多处）→ 改前先列影响面，说清这一处相关的前后章节、下游引用。
- **Chesterton's Fence**（拆一道篱笆前先搞清它为什么在）→ 改前先问这个点历史上动过没有，别推翻上次已定的事。
- **Kent Beck：Make the change easy, then make the easy change** → 当前骨架下改得很别扭时，别硬塞补丁；先调整骨架，让真正的改动变简单，再动手。
- **Outline-First**（骨架先立）→ 骨架先立、内容后填、过渡最后加，改了任何一节都回骨架重扫一遍。

落地成一个改前必走的流程：**通读整体 → 列影响面 → 三问过闸**（历史上动过吗？修法风险比原问题大吗？会破坏整体骨架吗？）。三问任一不过，就回退到「不改」或「回骨架重设计」，不许硬修。

它是**软约束**——不阻断交付，只在你动手前逼你先想三秒：这一处，我看清全局了吗？

## 安装

```bash
git clone https://github.com/Job-Yang/jobyang-ai-skills.git
cp -R jobyang-ai-skills/skills/sansi-erhouxing ~/.claude/skills/sansi-erhouxing
```

## 结构

```text
sansi-erhouxing/
├── SKILL.md                # 三步硬约束 + 骨架优先 + 改前通读报告模板
└── references/
    ├── doc-scope.md        # 通篇优化 / 新增章节时「整体」怎么界定
    └── prior-art.md        # 三条业界经典反模式的源头
```
