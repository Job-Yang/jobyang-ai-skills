# 三思而后行 · 改前通读的自律流程

一个前置动作 Skill：改任何已成形的文档之前，先通读全文骨架再动手，别只盯一段、别补丁摞补丁。

它是**软约束**，不阻断交付，只挡在「要动手改」这个动作前面。它跟写作 / 改稿技能**并行、不是二选一**：那些技能管「怎么写好」，这个技能管「动手前先看整体」——两层都走，先三思再动笔，别因为一句话里带了「润色」「精简」就把这一步跳了。

本技能只管文档类产物（文章、调研、方案、技能文档等成形文字）的修改。代码 / 工程改动不在范围内。

## 什么时候触发

- 改文档、改稿、补一段或一节、新增章节。
- 精简 / 润色 / 优化 / 压缩 / 扩写已有段落。
- 按评审意见 / 反馈意见改稿。
- 迭代或更新一个已有技能文档、方案、配置说明。

不触发：从零新起一篇、纯一句话答疑、明确说「随手写个一次性草稿」。

## 三步硬约束（改前必走）

底子是三条业界经典：Shotgun Surgery（改一处要连带看多处）、Chesterton's Fence（拆之前先搞清它为什么在）、Outline-First（骨架先立）。

1. **通读整体**：读到位，不到位不许下笔。
2. **列影响面**：这次要动的点 + 跟它相关的既有逻辑。
3. **三问过闸**：历史上动过吗？修法风险比原问题大吗？会破坏整体骨架吗？任一不过就回退到「不改」或「重设计」。

## 安装

```bash
git clone https://github.com/Job-Yang/jobbyang-ai-skills.git
cp -R jobbyang-ai-skills/skills/sansi-erhouxing ~/.claude/skills/sansi-erhouxing
```

## 结构

```text
sansi-erhouxing/
├── SKILL.md                # 三步硬约束 + 骨架优先 + 改前通读报告模板
└── references/
    ├── doc-scope.md        # 通篇优化 / 新增章节时「整体」的判定细则
    └── prior-art.md        # 三条业界经典反模式的源头
```
