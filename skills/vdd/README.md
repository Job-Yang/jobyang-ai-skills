# VDD · 面向验证的开发

一个让 Agent 在下结论前先自证的 Skill：在说「做完了 / 没问题 / 我懂了」之前，先逼自己走一遍验证。

它同时治两个方向相反的毛病：一个是**爱报喜**——没验过就说做好了；一个是**硬挤问题**——为了显得尽责，把本来对的东西也说成问题、大改一通。VDD 用两根支柱把两边都摁住：说「完成」之前逼自己验一遍，面对多轮和大改动时守住一把不随对话漂移的尺子。

这套方法完全靠模型自己执行，不依赖任何外部工具或环境——这也是它作为「VDD 理论最小可复现证明」的关键：装上它，任何一个光秃秃的 Agent 都能立刻跑出「不乱报喜、不硬挤问题、敢说没问题」的效果。

## 什么时候触发

- 准备说「做完了 / 搞定了 / 应该没问题 / 我明白了」之前。
- 给出一个你没亲眼验过的结论、诊断、判断。
- 被要求「整体 review 一下」「从全局看看有没有问题」。
- 用户明确说「验一下」「你确定吗」「VDD 走一遍」「别急着说完成」。

## 核心方法

- **单次自证**：说完成前走三步——扒前提、每个动作给证据、过时间/范围/机制/反证四道关卡；并且换一个「验收者」身份去走，别用做事那颗脑子自我背书。
- **防漂移**：动手前先立一把尺子（系统底线 / 任务契约 / 单次验收三层），之后所有判断对照尺子，而不是对照用户最新那句话。评审时只给三种裁决：符合、偏离、尺子没覆盖。

## 安装

```bash
git clone https://github.com/Job-Yang/jobbyang-ai-skills.git
cp -R jobbyang-ai-skills/skills/vdd ~/.claude/skills/vdd
```

装好后，直接对 Agent 说这类话就会触发：

- 「这个改动 VDD 走一遍」
- 「你确定吗，验一下」
- 「整体 review 一下有没有问题」

## 结构

```text
vdd/
└── SKILL.md    # 方法主体：单次自证三步 + 防漂移三层尺子
```
