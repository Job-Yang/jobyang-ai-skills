# VDD · 面向验证的开发

[English](./README.en.md)

在说「做完了」「没问题」「我懂了」之前，先逼自己走一遍验证。

VDD（Verification-Driven Development，面向验证的开发）是一套方法，也是一个证明。这个技能装上去，任何一个光秃秃的 Agent 都能立刻跑出「不乱报喜、不硬挤问题、敢说没问题」的效果——理论有没有用，你自己跑一遍就知道。它完全靠模型自己执行，不依赖任何外部工具或环境，这是有意的：VDD 的效力必须来自方法本身，而不是来自谁给你搭好的架子。

## 它在防什么

**第一个毛病：爱报喜。**

模型天生太想让用户满意，这是训练出来的本能。副作用是，它会不由自主地把「做完了」「我懂了」说出口，哪怕其实没验过。倒不是存心骗人，顺着自己那条思路走到这儿，它是真觉得没毛病了。

问题在于：**你没法用「做这件事」的那条思路，去可信地判断「这件事到底做成没有」。** 自己既当运动员又当裁判，结果就不可信。人类社会早懂这个道理——代码写完要交给别人 review，开发完还要有独立 QA，根本原因就一句话：没有人能有效验收自己的产物。模型还多一层麻烦：它说的每句话都接着前文往下写，先给了结论，再让它自查，这次自查还是顺着刚才那条被前文钉死的思路走，越认真越是在原路上加固。

**第二个毛病：硬挤问题、见风使舵。**

一旦你让模型带着「必须找出问题」的心态去 review，它几乎一定能挤出一堆「问题」——哪怕东西本来是对的。大改动、多轮对话里最要命：一个大版本让它从全局看看有没有问题，它出于「得找出点东西才算尽责」的本能，把合理设计也说成问题，大刀阔斧改一通，一测一堆新 bug；你问是不是改多了，它又立刻反过来认「确实过度了，要回退」。一会儿嫌改多、一会儿嫌改少，尺度永远飘。

这两个毛病其实是同一个病的两张脸：**报喜是讨好，硬挤问题、见风使舵同样是讨好。** 前者顺着「你希望它做成了」，后者顺着「你希望它找出问题」，两个方向都是被当下这句话牵着走。

## 它怎么治

VDD 有两根支柱，缺一不可。

**支柱一 · 单次自证：说完成之前走三步。**

- **扒前提**：先别急着接活，问清楚这个要求藏着哪些没说出口的假设、缺哪些信息、这类事别人常在哪儿栽跟头。问题问歪了，后面做得再漂亮也是错的。
- **给证据**：每改一处、每下一个判断都要能说出为什么，而且是拿得出证据的为什么。逼你给理由，就是逼你去找证据；证据还要分级、标清来源，**宁可老实标上「这条是我推的、没亲眼看到」，也绝不许把推断当观测讲出去**。
- **过四关**：时间对得上、范围对得上、机制说得通、有反证。这四关是「怎样才算真的搞清楚一件事」拆开后的四个必答项，法官断案、医生确诊问的其实是同一批问题。

关键还有一步：**换一个身份去验。** 只是嘴上说「我再检查一遍」然后顺原思路再走一遍，那不叫验证，叫给自己的结论又背书了一次。必须把「做事的我」放下，当自己是一个第一次看到这份产物、并不知道它怎么做出来的验收者——最好开一轮干净对话，只带产物和验收标准过去，绝不把「我当时为什么这么做」一起带过去。

**支柱二 · 防漂移：给多轮和大改动立一把尺子。**

单次自证管「这一步做对没有」，但一件事跨了很多轮、或一次改动特别大时它管不住——目标会一点点漂，因为你每一轮的参照物都是用户最新那句话。治法是**动手之前先立一把尺子**，分三层，从慢变到快变：

- **系统底线**（最慢变）：整个系统不能破的规矩，跨所有任务长期有效。
- **任务契约**（一个大任务一份）：这次的核心目标、核心设计决策、明确的不改边界，动手前立、干完作废。
- **单次验收**（每一步）：就是支柱一那套标准。

之后所有判断都对照这把尺子，而不是对照当下这句话。评审时**只给三种裁决**：符合尺子就明说「不用改」（不需要为了显得尽责而找问题）、偏离尺子就给证据改回、尺子没覆盖就停手问用户。每一条想报的「问题」都必须对应到尺子上的某一条，对应不上的就不是 bug，最多是「尺子的缺口」，另记下来跟用户讨论。这一条同时挡住了过度修改和硬编问题。

尺子会过时，但只能**显式地改**：先改尺子、经用户确认、再动手，绝不许先按新想法改完、回头再把尺子改成「我本来就该这么做」。

## 为什么它是「VDD 理论的最小可复现证明」

VDD 这套理论光讲道理，别人会问「真有用吗」。这个技能就是拿来当场验的：它不借助任何外部工具，纯靠方法本身，就能让一个普通 Agent 立刻表现出「敢说做成了、敢说没做成、也敢说没问题」。你按它走一遍，就亲身验到了这套理论确实成立——这本身就是 VDD 精神的示范：**不靠嘴说，靠走一遍拿到证据。**

## 安装

```bash
git clone https://github.com/Job-Yang/jobyang-ai-skills.git
cp -R jobyang-ai-skills/skills/vdd ~/.claude/skills/vdd
```

## 结构

```text
vdd/
└── SKILL.md    # 单次自证三步 + 防漂移三层尺子，方法完整自包含
```
